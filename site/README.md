Generated website output folder.

This folder is intended to be fully replaceable during generation/deploy.

Suggested usage:
- `site/` contains generated HTML and deploy assets
- `shared-assets/` contains canonical source images/data used by tools and generation
